<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Type-specific accents -->
    <SolidColorBrush x:Key="AccentInfo" Color="#6495ED"/>
    <!-- CornflowerBlue -->
    <SolidColorBrush x:Key="AccentSuccess" Color="#228B22"/>
    <!-- ForestGreen -->
    <SolidColorBrush x:Key="AccentError" Color="#CD5C5C"/>
    <!-- IndianRed -->

    <!-- Dialog Button Style -->
    <Style x:Key="DialogButton" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource B_Surface}"/>
        <Setter Property="Foreground" Value="{DynamicResource B_Text}"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="Height" Value="36"/>
        <Setter Property="MinWidth" Value="100"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="#444"/>
        <Setter Property="Margin" Value="6,0,0,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="10,0"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="#888"/>
                <Setter Property="Background" Value="#3A3A3A"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#1E1E1E"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Base MessageBox Style -->
    <Style x:Key="BaseMessageBox" TargetType="Border">
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Padding" Value="20"/>
        <Setter Property="Background" Value="{DynamicResource B_BackgroundPrimary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource B_Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <!-- Standard MessageBox Style -->
    <Style x:Key="MessageBoxStandard" TargetType="Border" BasedOn="{StaticResource BaseMessageBox}">
        <Setter Property="MinWidth" Value="360"/>
        <Setter Property="MaxWidth" Value="400"/>
    </Style>

    <!-- Custom MessageBox Style -->
    <Style x:Key="MessageBoxCustom" TargetType="Border" BasedOn="{StaticResource BaseMessageBox}">
        <Setter Property="MinWidth" Value="400"/>
        <Setter Property="MaxWidth" Value="600"/>
    </Style>

    <!-- Prompt MessageBox Style -->
    <Style x:Key="MessageBoxPrompt" TargetType="Border" BasedOn="{StaticResource BaseMessageBox}">
        <Setter Property="MinWidth" Value="400"/>
        <Setter Property="MaxWidth" Value="520"/>
    </Style>

    <!-- Message TextBlock -->
    <Style x:Key="DialogText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <!-- Input textbox -->
    <Style x:Key="DialogInput" TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource B_Surface}"/>
        <Setter Property="Foreground" Value="{DynamicResource B_Text}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource B_Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="Margin" Value="0,8,0,12"/>
    </Style>

</ResourceDictionary>